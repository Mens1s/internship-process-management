import React, { useState, ChangeEvent, FormEvent } from "react";
import styled from "styled-components";

const Evaluate: React.FC = () => {
  return <div>değerlendir</div>;
};

export default Evaluate;
